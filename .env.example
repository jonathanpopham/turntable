# Railway auth. Their canonical names and resolution order (explicit > env).
RAILWAY_API_TOKEN=

# The dedicated project and environment the managed container lives in.
# Blast radius is bounded to this project by construction.
TARGET_PROJECT_ID=
TARGET_ENVIRONMENT_ID=

# Basic auth for the deployed UI. The app is public and its button spends money.
APP_PASSWORD=

PORT=3000
